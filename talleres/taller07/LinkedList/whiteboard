Node reverse(Node *head){
  if (head == null) return false;

  Node *node = head, *anterior = null, *next;

  while(node != null){
    next = node -> next;
    node -> next = anterior;
    anterior = node;
    node = node -> next;
  }

  return anterior;
}

struct Node{
  int data;
  Node *next;
}
